# kinklessons.github.io

This repository hosts a static GitHub Pages site with beginner-friendly kink resources.

## Publishing

The site is deployed automatically when changes are pushed to `main` via GitHub Actions.

The workflow file is located at `.github/workflows/deploy-pages.yml`.

Once enabled in the repository settings, GitHub Pages will serve the site from the generated deployment.
